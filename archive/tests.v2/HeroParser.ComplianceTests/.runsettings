<?xml version="1.0" encoding="utf-8"?>
<RunSettings>
  <!-- Configuration for compliance test execution -->
  <RunConfiguration>
    <!-- Allow parallel execution for compliance tests -->
    <MaxCpuCount>0</MaxCpuCount>
    <!-- Standard timeout for compliance tests -->
    <TestSessionTimeout>300000</TestSessionTimeout>
  </RunConfiguration>

  <!-- Framework-specific settings for xUnit -->
  <xUnit>
    <!-- Enable parallel execution for compliance tests -->
    <ParallelizeAssembly>true</ParallelizeAssembly>
    <ParallelizeTestCollections>true</ParallelizeTestCollections>
    <!-- Use default thread count -->
    <MaxParallelThreads>default</MaxParallelThreads>
  </xUnit>

  <!-- Framework-specific settings for MSTest (if needed) -->
  <MSTest>
    <Parallelize>
      <Workers>0</Workers>
      <Scope>MethodLevel</Scope>
    </Parallelize>
  </MSTest>

  <!-- Framework-specific settings for NUnit (if needed) -->
  <NUnit>
    <NumberOfTestWorkers>-1</NumberOfTestWorkers>
  </NUnit>
</RunSettings>